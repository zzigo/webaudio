# Hydra Generators

```
osc(freq, sync, phase)
solid(r,g,b).out()
voronoi()
noise()
gradient()
shape() //sides, scale, border
scale() //

EXAMPLES
noise().out(o1)
osc().add(o1).out()

//primitives
shape(3).out(o0)

shape([3,4,5].fast(50)).rotate([1,2,3,4,5,6].fast(100)).out(o0)




```

#### osc

